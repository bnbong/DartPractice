import 'dart:ffi';

void main() {
    dynamic name;
    if (name is String) {
      // changed possible methods to string
    }
    if (name is int) {
      // changed possible methods to integer.
    }
}